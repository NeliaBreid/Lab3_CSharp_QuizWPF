<Window x:Class="QuizLab3.Dialogs.ResultDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:QuizLab3.Dialogs"
        mc:Ignorable="d"
        Title="ResultDialog" Height="280" Width="260" ResizeMode="NoResize">
    <Grid>
        <StackPanel Margin="10" HorizontalAlignment="Center" VerticalAlignment="center">
            <TextBlock Text="You completed the Quiz!" Margin="5" FontWeight="Bold"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock Text="You got: "/>
                <TextBlock Text="{Binding PlayerViewModel.CountCorrectAnswers, UpdateSourceTrigger=PropertyChanged}"></TextBlock>
                <TextBlock Text=" out of "/>
                <TextBlock Text="{Binding PlayerViewModel.TotalQuestions}"></TextBlock>
            </StackPanel>
            <Button Content="Go Back" Margin="20" HorizontalAlignment="Center" Command="{Binding ShowConfigurationViewCommand}" Click="Button_Click"/>
        </StackPanel>

    </Grid>
</Window>
